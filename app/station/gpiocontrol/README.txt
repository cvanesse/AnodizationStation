The goal of the gpiocontrol package is to allow simple programmatic control
over outpin pins using bus and pin abstractions.

It's also built to automatically detect wether or not it's on a raspberry pi
and use terminal test equivalents in the case that it's not
